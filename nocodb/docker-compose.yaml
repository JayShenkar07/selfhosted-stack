version: '2.1'

services:
  nocodb:
    image: nocodb/nocodb:latest
    restart: always
    ports:
      - "8080:8080"
    environment:
      NC_DB: "pg://<ip>:5432?u=postgres&p=<password>&d=nocodb"
    volumes:
      - "nc_data:/usr/app/data"

volumes:
  nc_data: {}
